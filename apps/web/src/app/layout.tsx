import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  subsets: ["latin"],
  variable: "--font-geist-sans",
});

const geistMono = Geist_Mono({
  subsets: ["latin"],
  variable: "--font-geist-mono",
});

export const metadata: Metadata = {
  title: "AI Spend Audit — Stop overpaying for AI tools",
  description:
    "Analyze your AI tool subscriptions in 2 minutes. Find exactly how much you're overspending and get actionable recommendations.",
  openGraph: {
    title: "AI Spend Audit — Stop overpaying for AI tools",
    description: "Free audit. Instant results. No signup required.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
